# Archived Log Entries — 2026-07

> Moved verbatim from `.agents/memory/MEMORY.md` during `/save-memory` archiving
> (oldest entries beyond the ~15-entry window). Do not rewrite or summarize.

- **2026-07-10**: Implemented Client-side Lazy Loading (Infinite Scroll) for Daily Discover Grid.
  - **Changed**: Extracted static server-side Daily Discover grid into a Client Component `src/components/home/DailyDiscover.tsx`. Implemented batch rendering (24 products initially, followed by 18 products per scroll batch) triggered by an `IntersectionObserver` with a styled blue spinning loader and simulated 600ms latency. Passed dynamic localized parameters from `page.tsx` and resolved TypeScript assignment errors.
  - **Why**: To prevent homepage lag, handle large product volume scaling, and offer a premium Shopee-style dynamic loading UX on scroll.
  - **Gotchas**: Keep prop definitions typed strictly. Pass default fallback empty arrays (`products || []`) to satisfy TypeScript compiler limits.
  - **Open threads**: None.

- **2026-07-10**: Overhauled homepage layout with featured highlight row panels.
  - **Changed**: Restricted homepage category row panel displays to only featured tech categories (`computer` and `mobile`), displaying 5 products + 1 view more card. Omitted other categories (`beauty` and `clothing`) from vertical row panels to make the homepage compact, leaving them accessible via bubble links and the unified Daily Discover feed.
  - **Why**: To prevent homepage layout redundancy and excessive vertical scroll, implementing a Shopee-style Highlight Row Panel design.
  - **Gotchas**: None.
  - **Open threads**: None.

- **2026-07-10**: Overhauled homepage layout with premium, responsive e-commerce sections.
  - **Changed**: Restructured `src/app/[locale]/page.tsx` with dynamic auto-fit categories grid, interactive promos, horizontal Flash Sale slider with countdown timer and sold progress bars, and a unified tabbed Daily Discover feed. Resolved React hooks purity rule warning (replaced `Math.random` with deterministic modulo). Deleted temporary scripts `check_db.js` and `check-columns.js` to ensure clean build.
  - **Why**: To address user feedback that the homepage layout did not look like a realistic online shop, and dynamic categories/feeds were needed.
  - **Gotchas**: React 19's strict checks forbid calling impure functions like `Math.random()` during component render. Hashing dynamic attributes (like product title length) is a clean, pure solution for mock parameters.
  - **Open threads**: None.

- **2026-07-10**: Renamed project to Lin Let and updated primary theme color to blue.
  - **Changed**: Rebranded occurrences of "shopMM" to "Lin Let" in `layout.tsx`, `Header.tsx`, `Footer.tsx`, `send.ts` (email config), and updated localStorage key in `CartProvider.tsx`. Switched Emerald Green color variables to Blue variants in `globals.css`. Fixed ESLint / TypeScript strict errors in `page.tsx`, `HeroSlider.tsx`, `request.ts`, and `middleware.ts`. Reduced section top/bottom padding in `globals.css` from 80px to 32px to resolve the excessive spacing under the navigation bar.
  - **Why**: To rename the project from shopMM to Lin Let, update the brand colors as requested, and correct vertical alignment spacing.
  - **Gotchas**: None.
  - **Open threads**: None.

- **2026-07-10**: Overhauled products category page UI/UX for responsiveness and visual aesthetics.
  - **Changed**: Removed inline styles and added responsive container styling in `src/app/[locale]/(shop)/products/page.tsx` and `src/app/globals.css`. Restructured mobile navigation to use horizontal scrolling chips for categories and stacked the header bar on mobile to avoid overflow. Redesigned product cards with scale-zoom, hover lift effects, and clamped title heights.
  - **Why**: To deliver a premium, app-like browsing experience, solve critical mobile grid breaking layout issues, and ensure uniform product card formatting.
  - **Gotchas**: Overcrowded header elements on mobile (logo, search, action buttons) required splitting into a two-row responsive wrap layout to fully eliminate horizontal window scrolling.
  - **Open threads**: None.

- **2026-07-10**: Updated beauty products with high-quality, category-specific Unsplash images.
  - **Changed**: Ran database update script to replace low-quality/incorrect dummy images for all 30 beauty products in the database with high-resolution Unsplash photos tailored to product titles (lipstick, mascara, eyeshadow, foundation, blush).
  - **Why**: To address the user's request regarding simple/incorrect images for the beauty category and to ensure the products showcase realistic, premium-looking photos.
  - **Gotchas**: None.
  - **Open threads**: None.

- **2026-07-10**: Redesigned homepage UI to group products by category and seeded realistic e-commerce data.
  - **Changed**: Modified `src/app/[locale]/page.tsx` to group product displays by category (1 row of 6 columns, with a "View more" card). Added strict typings for `productsByCategory` to resolve build errors. Restored hardcoded Unsplash mapping for category icon thumbnails. Seeded database with ~480 realistic products across 16 categories and removed `jewelry`, `sports`, `automotive`, and `pet-supplies`.
  - **Why**: To significantly improve the UI aesthetics and user experience by organizing products logically on the homepage, and ensuring dummy data looks professional and contextually accurate.
  - **Gotchas**: When manipulating `productsByCategory` which aggregates database outputs, strict typings (`Record<string, { id: string; ... }[]>`) are essential to avoid Next.js/TypeScript build failures. Relying on random images for category icons was flaky; hardcoding high-quality Unsplash URLs for categories ensures perfect presentation.
  - **Open threads**: None.

- **2026-07-10**: Fixed all strict lint and build errors, initialized Git repository for team-10-app.
  - **Changed**: Replaced strict `any` types with explicit typing in `action-split.ts`, `action-orders.ts`, `CartProvider.tsx`, `AddToCartButton.tsx`, `orders/page.tsx` and others. Formatted entire codebase using Prettier. Initialized git repository locally and linked to remote `https://github.com/vibe-code-tours/team-10-app.git`.
  - **Why**: To ensure the project is fully clean for production build and is linked to the requested remote team repository.
  - **Gotchas**: Next.js App Router heavily relies on strict Supabase TypeScript generation. Dealing with inferred arrays from single-row relationships (like `products` joined from `order_items`) required explicit type union to appease Next.js' strict build checks.
  - **Open threads**: Push changes to remote.

- **2026-07-09**: Implemented Search, Filtering, Authentication, Reviews, and User Profiles.
  - **Changed:** Added global search bar and sort/filter dropdowns to products page. Built order history (`/account/orders`), product reviews component (`/products/[id]`), and a dedicated profile settings page (`/account/settings`). Added database migrations for `public.users` (with auth trigger) and `public.product_reviews`, and modified the `orders` table to include `user_id`. Updated the project directory to `Lin Let`.
  - **Why:** To satisfy user requests for Search & Filtering (Feature 4), Customer Authentication & Order History (Feature 5), Reviews & Ratings (Feature 6), and a User Profile Settings page.
  - **Gotchas:** The Supabase CLI was not linked to the remote project, so database migrations (creating `users`, `product_reviews`, modifying `orders`) had to be run manually by the user in the Supabase SQL Editor via provided SQL scripts in the walkthroughs.
  - **Open threads:** None.

- **2026-07-09**: Initialized AI agent configurations.
  - **Changed:** Copied and adapted `.agents` and `.claude` directories from the previous project to fit the online-shop structure.
  - **Why:** To bootstrap the AI agent workflows and provide project-specific context.
  - **Gotchas:** None.
  - **Open threads:** Git repository name and remote setup are pending.
